<section class="content-section about">
    <div class="about__inner">
        {{> title text="Меня зовут" subText=fullName color="_white"}}
        <h2 class="about__side-title">Коротко о себе</h2>

        {{#each text.about}}
            {{> titleDesc text=this}}
        {{/each}}

        {{>number number="01" class="_about"}}
    </div>
</section>